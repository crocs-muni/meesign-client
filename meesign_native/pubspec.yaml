name: meesign_native
description: Dart wrappers around MeeSign Rust and C++ libraries.
version: 0.1.1
publish_to: none

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  ffi: ^1.1.2

dev_dependencies:
  ffigen: ^6.0.0
  lints: ^2.0.0
  test: ^1.16.0
